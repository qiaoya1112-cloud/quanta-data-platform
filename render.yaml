services:
  - type: web
    name: quanta-data-platform
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn data_platform:app --bind 0.0.0.0:$PORT
    autoDeploy: true
